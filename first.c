#include<stdio.h>

void main()
		{
			int a,b;
			char c;        
	printf("Чо над сделац?" + - \ * "\ n");
while(l)
	{
	printf("Оператор ввода:");
	scanf("%c" ,&c);
	printf("Введи два числа:");
	scanf("%d %d",&a,&b);
switch(c)
	{
		case '+' :
		printf("%d+%d=%d\n",a,b,a+b);
	break;
			case '-' :
			printf("%d-%d=%d\n",a,b,a-b);
		break;
				case "*" :
				printf("%d*%d=%d\n",a,b,a*b);
			break;
					case "/":
					printf("%d\%d=%d\n",a,b,a/b);
				break;
					default:
		printf("input error\n");
                      }
getchar();
	}
		}

